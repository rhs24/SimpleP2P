# Student Resource Sharing System (P2P)

A decentralized, peer-to-peer file sharing application built in Java. This project demonstrates core **Distributed Systems** concepts like peer discovery, socket programming, and in-memory data management.

---

## 🚀 How to Run the Demo

### 1. Prerequisites
* **Java JDK 8 or higher** installed.
* Two or more terminal windows open (to simulate different nodes).

### 2. Compilation
Compile all files using the command line:
```bash
javac *.java
